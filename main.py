import numpy as np

def main():
    subset = distance.iloc[np.append(city_index, locations),np.append(city_index, locations)]
    x = DoubleList()
    hotel = Node([0, 0, 0], "Hotel")
    x.add(hotel)
    replicate = 50
    for k in range(0,replicate):
        x.addTry()












